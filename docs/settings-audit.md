# 設定項目表 H監査結果（D1〜D3 標準ノード 24本）

スタイルガイド カテゴリ H（設定項目の表記と説明）に対する、D1〜D3 フェーズ 24 本の監査記録。
各ノードのガイド記載名を、Node-RED ソース（`<node>.html` の `data-i18n` キー）と
locale（`locales/ja/messages.json`）で解決した**エディター実ラベル**と突き合わせた。

重要：「D1〜D3 適用済み」はもともと**文章推敲（B〜G）**の記録であり、設定項目表（H）の
監査ではなかった。本監査の結果、24 本中**過半が実ラベルと不一致**（説明的な言い換え・英語ラベル・
存在しない項目）であることが判明した。下表が是正のチェックリストである。

監査日：2026-06-28／照合元：node-red master ＋ ja locale。

## ① 確実に要修正（実ラベル不一致・言い換え・英語・捏造） — 12本

| 状態 | ノード | 代表的な不一致（ガイド → エディター実ラベル） |
| --- | --- | --- |
| [x] | debug | 出力対象→**対象**／「出力先」3行→**デバッグウィンドウ・システムコンソール・ノードステータス(32 文字)**（修正済 bf96845） |
| [x] | delay | 遅延時間→**時間**／中間メッセージを破棄→**削除**／レート上書き許可→**msg.rate(ミリ秒単位)で流量値を上書き**／レート→**流量**／トピックごと→**msg.topic毎**（修正済 8be6c8e） |
| [x] | exec | msg.payloadを追加→**引数**／「旧式の出力」は**実在しない**（削除）／**追加引数・コンソールを非表示・名前**を補完（修正済 65709ae） |
| [x] | filter | モード→**動作**（6選択肢を実ラベル化）／トピックごとに比較→**個別に動作を適用**／捏造「出力タイミング」削除／比較対象を補完（修正済 8be6c8e） |
| [x] | inject | 「追加プロパティ」は**実在しない**（プロパティ一覧として説明）／指定した時刻→**繰り返しの実選択肢**／Node-RED起動時にも実行→**Node-RED起動の〜秒後、以下を行う**（修正済 65709ae） |
| [x] | join | 設定項目をモード別（共通/手動/列の集約）に再構成。対象プロパティ→**結合**／合計値→**指定数のメッセージパーツを受信後**／タイムアウト→**最初のメッセージ受信からのタイムアウト後**／蓄積モード→**後続のメッセージ毎**／Reduce式→**集約式**／最終処理→**最終調整式**／逆順で評価→**評価を逆順に行う**。出力5選択肢を実ラベル化、名前・msg.complete を補完（修正済 5c1586f） |
| [x] | sort | ソート対象→**対象**／ソート順→**順序**／数値として→**数値として比較**（修正済 4957ad0） |
| [x] | change | 設定項目を「共通/操作の種類/各ルールの入力欄」へ再構成（ルールは動的生成）。操作タイプ→**操作の種類（値の代入/値の置換/値の削除/値の移動）**／値→**代入する値**／検索文字列→**検索する文字列**／置換文字列→**置換後の文字列**。名前・値のディープコピーを補完、プロパティタイプ行は対象プロパティへ統合（修正済 9867a8a） |
| [x] | html | Selector/Output→**プロパティ/抽出する要素/出力**／「出力」4選択肢を実ラベル化（修正済 c47f471） |
| [x] | range | Property/Action/Input Range→**プロパティ/動作/入力値の範囲**／「動作」4選択肢を実ラベル化。計算値も訂正（150→scale 383・roll 128、Math.round 切り上げ）（修正済 c910595） |
| [x] | file | 既定値とオプション表を実ラベル化（append to file→**ファイルへ追記**、a single utf8 string→**文字列** 等）。「名前」「各メッセージに既存の全プロパティを含める」を補完。読込の文字コード既定 utf8→**デフォルト** に訂正（修正済 c706710） |
| [x] | complete-status | complete の Scope/Name を実フォーム（対象ノード選択リスト＋名前）へ。status の「ステータス取得元」3選択肢を補足（修正済 992652a） |

## ② 軽微な不一致 — 3本

| 状態 | ノード | 不一致 |
| --- | --- | --- |
| [x] | batch | 設定項目をモード別（共通/メッセージ数/時間間隔/列の結合）に再構成。オーバーラップ→**オーバラップ**（全体統一）／間隔（秒）→**時間間隔**／モード選択肢を実ラベル化、名前を補完（修正済 efc5a5a） |
| [x] | json | 整形出力→**JSON文字列フォーマット**。動作の3選択肢（相互変換／常にJSON文字列に変換／常にJavaScriptオブジェクトに変換）を実ラベルで明記、列を既定値に統一（修正済 0a419ba） |
| [x] | link | 捏造ラベル「Links」「Link」（実体は無題のツリー選択）を削除。Link Out のモード2択（接続された全てのlinkノードへ送信／link callノードへ返却）・Link Call のリンクの種類2択（対象を固定で指定／対象を動的に指定 (msg.target)）を実ラベル化。名前・既定値（タイムアウト30秒）を補完。本文・演習の Mode/Return→モード/「link callノードへ返却」も是正（修正済 d35fba2） |

## ③ 概ね準拠（軽微確認のみ） — 4本

| 状態 | ノード | 備考 |
| --- | --- | --- |
| [x] | catch | エラー取得元／Catchノードで処理済みのエラーを無視／名前 すべて一致 |
| [x] | http | メソッド／URL／名前／ステータスコード／ヘッダ 一致 |
| [x] | csv | ガイド独自の「パース時／生成時」分割を、エディター実構造（共通設定／CSVからオブジェクトへ変換／オブジェクトからCSVへ変換、入力・出力の区分）へ再構成。パーサ選択肢を実ラベル化（legacy→従来）、欠落していた名前を補完、各表に既定値列を追加。recipe・演習・まとめ・トラブルシュートの漏れも実ラベルへ（カラム名→列名、1行目をカラム名に→1行目に列名を含む、出力形式→出力 等）（修正済 300126a） |
| [x] | function | 英語の旧3タブ表記（Setup/Function/Close）を実エディターの4タブ（設定／初期化処理／コード／終了処理）へ是正。抜けていた「設定」タブ（出力数・タイムアウト・モジュール）を追加。タイムアウトの単位誤りを訂正（ミリ秒→秒）。表をタブ列・既定値付きに再構成（修正済 6670498） |

## ④ contrib・別方針が必要 — 4本

| 状態 | ノード | 備考 |
| --- | --- | --- |
| [x] | modbus | エディターは英語のみ（ja ロケール無し）。全項目を「英語ラベル（日本語訳）」形式へ。実タブ構造（Client は Settings / Queues / Optionals、Read 系は Settings / Optionals）でグループ化し、欠落していた Serial type・Connection delay (ms)・ASCII response start delimiter・Reconnect timeout (ms)・Queues タブ4項目・Log states changes / Log failures・Read の Delay 系と IO-File 系・Getter/Write/Flex の Keep Msg Properties 等を補完。ソース照合元は BiancoRoyal/node-red-contrib-modbus master（src/modbus-*.html ＋ en-US locale）（修正済 e62f0a2） |
| [x] | email | **エディターは大半が日本語化済み**だった（contrib だが英語ではない）。英語ラベル To/Server/Port/Name 等 → **宛先/サーバ/ポート/名前** 等の実日本語ラベルへ。送信に欠落の Auth type（認証方式）・TLSオプション、受信に メール受信/更新/TLSを開始?/Auth type/条件 を補完。受信の暗号化欄は実ラベル **SSLを使用**（送信の 安全な接続を使用 とは別）に訂正。受信後の処理を なし/既読/削除 に。locale 未翻訳の **Auth type/Token/Format to SASL** のみ「英語（日本語訳）」形式。MTA ノードは対象外（修正済 1ac2871） |
| [x] | base64 | **エディターは日本語化済み**だったため英語併記ではなく実日本語ラベルへ是正。捏造英語ラベル Action/Property/Name → **動作/プロパティ/名前**、選択肢 → **バッファ <-> Base64の変換（既定）/Base64へエンコード/Base64から文字列へ変換**（明示デコードは文字列出力、ランタイム確認）。本文・トラブルシュート・ベストプラクティスの漏れも是正（修正済 3fe1c52） |
| [x] | mqtt | 標準ノードでエディターは日本語化済み（ja ロケール完備）のため実日本語ラベルへ。ブローカー設定ノードを実タブ構造（接続／セキュリティ／メッセージ）で再構成し、欠落していた 名前・自動接続・TLSを使用・プロトコル・切断時に購読を自動解除・セッション有効期限・ユーザプロパティ(MQTT V5)、および Birth／Will／Close の3メッセージ欄（トピック・ペイロード・QoS・保持・遅延・V5メタデータ）を補完。クライアントID→**クライアント**・Keep Alive→**キープアライブ時間**・セッション初期化→**セッションの初期化** を是正。TLS は接続タブに属するため認証タブの記載を削除（セキュリティタブはユーザ名・パスワードのみ）。In に 動作・フラグ・Retainメッセージ処理・名前 を補い出力6選択肢を実ラベル化、Out に MQTT V5 メタデータ欄・名前 を補完。エディタ非表示の compatmode・topicAlias 等は除外。ソース照合元は node-red master（core/network/10-mqtt.html ＋ ja/en-US locale）（修正済 6423fdd） |

## 進め方

- ① から、これまでと同じ手順（ソース照合・2 本ずつ・1 ファイル＝ 1 コミット）で是正する。
- 完了した行は `[x]` に更新し、スタイルガイドの適用状況にも反映する。
- ②③④は ① 完了後に順次。④は contrib のため英語ラベル＋日本語訳の方針で扱う。
