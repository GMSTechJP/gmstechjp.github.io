# 追加ガイド 要件定義（バイナリ処理・ファイル転送・PLC・データ蓄積・本番運用）

今後追加・拡充するガイドの**要件の正本**。詳細な調査・設計・選定根拠は
[docs/roadmap.md](docs/roadmap.md) を参照。サイト全体の開発規約は
[CLAUDE.md](CLAUDE.md)、文章・設定項目の基準は
[docs/style-guide.md](docs/style-guide.md) を正とする。

作成日：2026-06-29
最終更新：2026-08-12（テーマ D・E の完了を反映）
ステータス：D・E 完了（2026-08-10）／A 着手中／B-1・B-2・C 未着手

---

## 1. 目的

2 つの施策を扱う。

**施策 1（テーマ A・B-1・B-2・C）**：バイナリデータ処理（Buffer・エンディアン）と
ファイル転送（FTP/SFTP）の理解を補う。3 テーマ（A・B-1・C）は「バイナリデータを
どう読み書きするか」で一本につながる学習動線とする。

**施策 2（テーマ D・E）**：製造業の IoT/DX 担当者が現場適用でつまずく
「取ったデータをどこに貯めるか」と「作ったフローを現場に置き続けられるか」を補う。
サイトの既存ガイドは Node-RED の操作とノード単体の解説がほぼ埋まった一方、
データ蓄積（DB）と本番運用は空白であり、この 2 テーマが最短の穴埋めとなる。

## 2. 対象スコープ（6 テーマ）

| ID | テーマ | 成果物 | 方針 |
| --- | --- | --- | --- |
| A | バイナリ/テキストと Buffer | 既存 `variable-types`（Part 1 軸）へ**追記** | 新規ファイルは作らない |
| B-1 | buffer-parser ノード | 新規ガイド | サードパーティ |
| B-2 | FTP / SFTP | 新規ガイド | server / client 両対応 |
| C | PLC レジスタ（エンディアン・Modbus 注意点） | **新規独立ガイド** | `modbus` 等から参照先とする |
| D | データ蓄積（SQLite） | 新規ガイド | サードパーティ。追加サーバー不要の DB 入口 |
| E | 本番運用（自動起動・バックアップ・寿命対策） | 新規ガイド | 概念＋手順。ノード非依存 |

## 3. 採用パッケージ（確定／npm 実調査に基づく）

| 用途 | パッケージ | 補足 |
| --- | --- | --- |
| B-1 変換 | `node-red-contrib-buffer-parser` | ビッグ/リトルエンディアン対応 |
| B-2 FTP クライアント | `node-red-contrib-ftp` | DL・star 最多 |
| B-2 FTP サーバー | `node-red-contrib-ftp-server` | DL・star 最多 |
| B-2 SFTP クライアント | `node-red-contrib-ssh-sftp`（第一）／ `node-red-contrib-sftp`（実績） | 前者は近年更新・ssh2-sftp-client ベース |
| B-2 SFTP サーバー | `node-red-contrib-sftp-server` | 選択肢が乏しい。OS の SSH デーモン代替も補足 |
| D データ蓄積 | `node-red-node-sqlite` | v2.0.1（2026-04-17 更新）。node-red-nodes 公式リポジトリ内。ja locale あり |
| E 本番運用 | パッケージ追加なし | 公式 `linux-installers` の `nodered.service` と付属スクリプトを一次情報とする |

着手時に各パッケージの最新版・DL 数・更新状況を再確認する。

## 4. 着手順

**D → E → A → B-1 → C → B-2**（B-2 に SFTP を含む）。

D・E は施策 2 として 2026-08-09 に先行着手する。施策 1（A・B-1・C・B-2）の
内部順序は当初方針のまま変更しない。

## 5. 品質要件（必須）

- 設定項目名はエディターの実表示に完全一致。ja locale が無い場合は
  「**English Label**（日本語訳）」形式。
- サンプルフローの JSON は `python3 -m json.tool` でパース成功すること。
- サードパーティガイドは「インストール方法・GitHub リンク・バージョン・免責」を明記。
- 設定項目の節はエージェントへ丸投げせず、1 ノードずつソースと挙動を確認して書く。
- フローティングホームボタン・前後リンク・コピーボタンを備える。
- 既存ガイドへの橋渡しリンクは実在ファイル名を指す。

## 6. 制約・注意

- 公開リポジトリ。機密情報・API キーを含めない。
- 平文 FTP は認証情報が平文で流れる。LAN 内・検証用に限定し、インターネット越しでは
  SFTP/FTPS を勧める注記を必須とする。
- FTP/SFTP サーバー系パッケージは更新が停滞している。トレードオフを本文に明記する。
- C は `modbus` ガイドと重複させない。アドレス体系（0 始まり・4xxxx）は既出のため、
  C は「レジスタ値のバイト解釈」に特化する。
- D は `sqlite3` のネイティブビルドを伴う。Raspberry Pi では導入に時間がかかり、
  Node.js 更新時の再ビルドも要る。この制約を本文に明記し、環境バッジは ☁️ にしない。
- E は OS 操作（systemd・journalctl）を含むため環境バッジは 🖥️ とする。既存の
  Linux 4 部作・`settings` ガイド・`git-basics` ガイドと相互リンクし、内容を重複させない。
- D と E は相互参照する（書き込み頻度と SD カード寿命の論点が両者にまたがるため、
  実体は E に置き D から参照する）。

## 7. 受け入れ基準

- 6 テーマの成果物が第 5 章の品質要件をすべて満たす。
- 第 2〜4 章の方針（追記 / 新規独立 / 採用パッケージ / 着手順）と一致する。
- 内容が [docs/roadmap.md](docs/roadmap.md) の設計と矛盾しない。
- `python3 scripts/validate-flow-json.py` が終了コード 0 を返す。

## 8. 変更履歴

| 日付 | 変更内容 | 理由 |
| --- | --- | --- |
| 2026-06-29 | 初版作成（テーマ A・B-1・B-2・C） | バイナリ処理とファイル転送の学習動線を定義 |
| 2026-08-09 | テーマ **D（データ蓄積・SQLite）** と **E（本番運用）** を追加。表題・第 1 章（施策 1／施策 2 の二本立て）・第 2 章スコープ表・第 3 章パッケージ表・第 4 章着手順・第 6 章制約・第 7 章受け入れ基準を更新 | 製造業の IoT/DX 担当者向けにサイトを見直した結果、「貯める」と「運用し続ける」が空白であると判明したため。利用者の判断（A 案）により正本を単一文書のまま拡張した |
| 2026-08-12 | ステータス行を更新（D・E を完了、A を着手中に変更） | D・E の成果物を 2026-08-10 に追加済みで、記載が実態と食い違っていたため |
| 2026-08-12 | 第 2 章スコープ表の A 行を「part2 軸」から「Part 1 軸」へ変更 | 追記先の前提が実ファイルと食い違っていたため。Part 2 は context 系変数（保存スコープ）が主題で Buffer 言及は 0 件、データ型の解説と Buffer 言及 4 件は Part 1 にある。利用者の判断（A 案）により追記先を Part 1 へ修正した |
